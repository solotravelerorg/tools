# SoloTraveler Tools (Jekyll)

Static tools library built with Jekyll. Tools run client-side (no backend).

## Run locally
```bash
bundle install
bundle exec jekyll serve
```

Open: http://localhost:4000
